name: Deploy to Freebox

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy via SSH with password
        uses: appleboy/ssh-action@master
        with:
          host: mon-suivis-sportif.freeboxos.fr
          username: freebox
          password: ${{ secrets.SSH_PASSWORD }}
          port: 49154
          script: |
            git config --global --add safe.directory /var/www/mon-suivi-sportif
            cd /var/www/mon-suivi-sportif
            git pull origin master
            npm install --omit=dev
            /usr/local/bin/pm2 startOrRestart ecosystem.config.js --update-env
            /usr/local/bin/pm2 save
            echo "✅ Déploiement réussi !"